import React from "react";
import { View, Text, StyleSheet } from "react-native";

const ResultsShowScreen = () => {
  return (
    <View>
      <Text> Results are shown here </Text>
    </View>
  );
};

const stles = StyleSheet.create({});

export default ResultsShowScreen;
